# Data-Quality Memo

**Author:** TODO — your name
**Dataset:** NASA Meteorite Landings (`data/meteorite_landings.csv`, see `data/SOURCE.md`)

One page. This memo isn't a restatement of what `analysis.ipynb` found — it's
about how much a downstream analyst should **trust** those findings before
acting on them.

## At least 2 specific, checkable questions

Pose real questions about this dataset's origin, completeness, or possible
bias — something the people who actually maintain it could answer with a
real, concrete fact. Not "is this data good?" — a question a grader (or a
real data steward) could actually imagine getting a yes/no or specific
answer to.

1. TODO
2. TODO

> ⚠️ A couple of things you ran into in `analysis.ipynb` are worth
> revisiting here, if you haven't already turned one of them into a
> question above: the `reclat`/`reclong` spike at exactly 0, and the
> `Fell` vs. `Found` split you used in Part 3. `data/SOURCE.md` documents
> a few more raw data-quality wrinkles if you want a fuller list — but the
> questions themselves should be yours, not copied from that file.

## Why this matters for the pattern you found

In 1-2 sentences: connect **at least one** of your questions above to
whether you'd actually trust the non-obvious pattern you wrote up in
`analysis.ipynb`'s Part 3. If the answer to your question turned out badly,
would it change the conclusion?

TODO
